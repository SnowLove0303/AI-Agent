## Design

1. 在 Section 2 共享策略中扫描 Section 3 证据，区分“有组分 GHS 证据”与“产品级无分类”。标准 fallback 只在没有冲突组分证据时允许。
2. 在同一值路由入口净化标签要素：识别特殊物质注意说明，删除独立的分类/H 编码行，并把独立阈值行并入前一条注意说明；最终预审和落盘审计都重复检查。
3. 在值单元格语义拓扑解析之后检查直接 OOXML 属性。值运行必须显式携带四类字体映射、`w:sz`、`w:szCs`；值段落垂直居中、普通值左对齐，S3 数据值居中。
4. 复用既有锁定骨架比较器和三列写入白名单，新增局部规则登记，使 Section 10 的序号+标签和 Section 11.2 的中间粗体子标签受同一全局闸门约束。

## Verification

- Section 2 组分证据、标签净化和最终 release gate 回归测试。
- S3/Section 11 值格式显式属性回归测试。
- `pytest -q` 在 `MSDS Skill` 维护测试根目录执行；F 盘产物删除后不宣称完成实物 DOCX 重放。
