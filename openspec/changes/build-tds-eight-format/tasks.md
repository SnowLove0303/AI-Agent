# Tasks

- [ ] 建立独立 `TDS Skill` 目录、版本文档和 manifest。
- [ ] 复制并登记四个用户 TDS `.doc` 模板的来源路径、大小和 SHA-256。
- [ ] 将四个 `.doc` 转换为 active `.docx`，记录转换工具和 hash。
- [ ] 对四个 active 模板执行表格/合并/grid width/段落字符属性/页眉页脚/包部件结构快照与几何审计。
- [ ] 建立统一 TDS semantic model、字段别名、四变体 registry、映射白名单和 fail-closed 规则。
- [ ] 实现一次抽取、一次映射、四模板 fresh clone 原位覆写的 CLI。
- [ ] 实现 DOCX→PDF 单一路径、DOCX/PDF 配对追溯和八文件包审计。
- [ ] 迁移可复用的 TDS 测试模式，补齐四变体模板基线、映射、示例事实隔离、PDF 派生和 ZIP 完整性测试。
- [ ] 用真实源文件回放；若源数据与模板容量不兼容，保留失败证据，不以模板示例事实代替。
- [ ] 运行 OpenSpec 校验、TDS 全部自动测试、MSDS/V2.9 继承审计、模板 geometry audit、semantic/company parity、八文件/ZIP 完整性审计。
- [ ] 生成独立 TDS v1.0 发布 ZIP，提交并推送 `feature/msds-tds-eight-format`。

